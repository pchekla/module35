@{
    ViewData["Title"] = "Главная страница";
}

<div class="container fade-in">
    <div class="row">
        <div class="col-md-12">
            <div class="card animate-on-scroll">
                <div class="card-body">
                    <h2 class="card-title">Добро пожаловать в <span class="main-logo-text">Cosmo<span class="main-logo-accent">Bazar</span></span>!</h2>
                    <p class="card-text">Вы успешно вошли в систему.</p>
                    
                    <div class="row mt-4">
                        <div class="col-md-6 animate-on-scroll">
                            <h4>Это главная страница для авторизованных пользователей.</h4>
                            <p>Здесь будет размещаться основной функционал социальной сети.</p>
                            
                            <form method="post" asp-controller="AccountManager" asp-action="Logout" class="mt-4">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-danger">Выйти</button>
                            </form>
                        </div>
                        <div class="col-md-6 animate-on-scroll">
                            <div class="image-container">
                                <img src="~/img/logo.png" alt="Логотип CosmoBazar" class="responsive-image image-hover" />
                                <p class="text-center text-muted mt-2">Наш адаптивный логотип</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 